#include<iostream> 
#include<bits/stdc++.h>
using namespace std;


// // Function --> Pass by value and pass by reference
// void doSomething (string s) {
//         s[0] = 'T';
//         cout << s << endl; // Output will be "Tarmik"
// }

// void doSomethingRef (string &s) {
//         s[0] = 'T';
//         cout << s << endl; // Output will be "Tarmik"
// }
int main() {
    // cout << "Hello, World!" << endl;
    // string s = "Marmik";
    // cout << s << endl;
    // int len = s.length();
    // cout << len<< endl;
    // s[len - 1] = 's';
    // cout<< s << endl;
  


   

    
    // string name = "Marmik";
    // doSomething(name);
    // cout << name << endl; // Output will be "Marmik" because string is passed by value
    // doSomethingRef(name);   
    // cout << name << endl; // Output will be "Tarmik" because string is passed by reference
    return 0;
}